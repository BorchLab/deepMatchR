% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toSerology.R
\name{toSerology}
\alias{toSerology}
\alias{.toSerologyGenotype}
\alias{.convertSingleAllele}
\alias{.parseAllele}
\alias{.getSerologyPrefix}
\alias{.lookupSerology}
\alias{.resolvePGroup}
\alias{.resolveBroadToSplit}
\alias{.handleUnknown}
\alias{.loadWmdaData}
\alias{.getWmdaCacheDir}
\title{Convert HLA Alleles to Serological Equivalents}
\usage{
toSerology(
  x,
  locus = NULL,
  resolve_splits = TRUE,
  return = c("serology", "genotype", "data.frame"),
  na_action = c("NA", "warn", "error")
)

.toSerologyGenotype(geno, locus, resolve_splits, return, na_action)

.convertSingleAllele(
  allele,
  locus_override,
  resolve_splits,
  na_action,
  wmda_data
)

.parseAllele(allele)

.getSerologyPrefix(locus)

.lookupSerology(locus, allele_2f, serology_db)

.resolvePGroup(locus, allele_2f, pgroups_db)

.resolveBroadToSplit(input_locus, allele_2f, serology, wmda_data)

.handleUnknown(allele, na_action, message)

.loadWmdaData()

.getWmdaCacheDir()
}
\arguments{
\item{x}{Character vector of HLA alleles OR an \code{hla_genotype} object.}

\item{locus}{Optional character. Override locus detection for ambiguous cases.}

\item{resolve_splits}{Logical. If TRUE (default), attempts to infer split antigens
from broad antigens (e.g., DR2 -> DR15 or DR16 based on the allele).}

\item{return}{Character. Output format:
\itemize{
  \item \code{"serology"} (default): Returns character vector of serology strings
  \item \code{"genotype"}: Returns hla_genotype with \code{*_ser_*} columns added
  \item \code{"data.frame"}: Returns data.frame with allele, locus, serology columns
}}

\item{na_action}{Character. How to handle alleles without serology mapping:
\itemize{
  \item \code{"NA"} (default): Return NA for unknown alleles
  \item \code{"warn"}: Return NA and issue a warning
  \item \code{"error"}: Stop with an error
}}
}
\value{
Depends on \code{return} parameter. See above.
}
\description{
Converts HLA alleles in IMGT/HLA nomenclature (e.g., "A*01:01") to their
serological equivalents (e.g., "A1") using WMDA standard nomenclature.
}
\details{
This function uses the WMDA (World Marrow Donor Association) nomenclature files
to map HLA alleles to their serological equivalents. The mapping considers:
\itemize{
  \item Unambiguous assignments (highest priority)
  \item Possible assignments
  \item Assumed assignments
  \item Expert assignments
}

When \code{resolve_splits = TRUE}, broad antigens like DR2 are resolved to their
split antigens (DR15 or DR16) based on the specific allele.

P-group notation (e.g., "A*01:01P") is automatically resolved to the reference allele.
}
\section{Functions}{
\itemize{
\item \code{.toSerologyGenotype()}: Process hla_genotype object

\item \code{.convertSingleAllele()}: Convert a single allele to serology

\item \code{.parseAllele()}: Parse allele into components

\item \code{.getSerologyPrefix()}: Get serology prefix for locus

\item \code{.lookupSerology()}: Look up serology in database

\item \code{.resolvePGroup()}: Resolve P-group to reference allele

\item \code{.resolveBroadToSplit()}: Resolve broad antigen to split

\item \code{.handleUnknown()}: Handle unknown allele based on na_action

\item \code{.loadWmdaData()}: Load WMDA data from package or cache

\item \code{.getWmdaCacheDir()}: Get WMDA cache directory

}}
\examples{
# Single allele conversion
toSerology("A*01:01")

# Vector of alleles
toSerology(c("A*01:01", "B*07:02", "DRB1*03:01"))

# Get detailed data.frame
toSerology(c("A*01:01", "B*07:02"), return = "data.frame")

# With hla_genotype object
geno <- hlaGeno(data.frame(
  A_1 = "A*01:01", A_2 = "A*02:01",
  B_1 = "B*07:02", B_2 = "B*08:01",
  stringsAsFactors = FALSE
))
toSerology(geno, return = "serology")
toSerology(geno, return = "genotype")

}
