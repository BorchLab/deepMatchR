% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/updateWmdaData.R
\name{updateWmdaData}
\alias{updateWmdaData}
\title{Update WMDA Nomenclature Data}
\usage{
updateWmdaData(
  version = "Latest",
  cache_dir = NULL,
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{version}{Character. The IMGT/HLA version to download. Default is "Latest"
for the most recent release. Can also be a specific version tag (e.g., "3.54.0").}

\item{cache_dir}{Character. Directory to store cached data. If NULL (default),
uses the package's default cache location.}

\item{force}{Logical. If TRUE, re-download even if cached data exists.}

\item{verbose}{Logical. If TRUE (default), print progress messages.}
}
\value{
Invisibly returns the path to the cache directory.
}
\description{
Downloads fresh WMDA nomenclature data from the IMGT/HLA GitHub repository
and caches it locally for use by \code{\link{toSerology}}.
}
\details{
The WMDA nomenclature files are downloaded from:
\url{https://github.com/ANHIG/IMGTHLA}

Downloaded files:
\itemize{
  \item \code{rel_dna_ser.txt}: DNA to serology mappings
  \item \code{rel_ser_ser.txt}: Broad to split relationships
  \item \code{hla_nom_p.txt}: P-group definitions
}

The cache is stored as a single RDS file for fast loading. To clear the cache
and revert to bundled data, delete the cache directory or set the environment
variable \code{DEEPMATCHR_CACHE_DIR} to a new location.
}
\examples{
# Show default cache directory (does not download)
cache_dir <- file.path(tempdir(), "wmda_test")
print(cache_dir)

\donttest{
# Update to latest WMDA data (requires internet)
updateWmdaData()

# Force re-download even if cache exists
updateWmdaData(force = TRUE)
}

}
\seealso{
\code{\link{toSerology}}
}
